# Postgres connection string for the helpdesk database.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/helpdesk"

# Isolated database for Playwright e2e tests. MUST be a DIFFERENT database from
# DATABASE_URL and its name must contain "test". Playwright resets this database
# on every run (see e2e/global-setup.ts), so never point it at real data.
# If omitted, it is derived from DATABASE_URL by appending a "_test" suffix.
TEST_DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/helpdesk_test"

# Better Auth: secret used to sign session cookies/tokens. Generate one with
# `openssl rand -base64 32`. Required in production.
BETTER_AUTH_SECRET=""
# Base URL the auth server is reached at.
BETTER_AUTH_URL="http://localhost:3000"

# Account created by `bun run db:seed`. SEED_USER_ROLE is "admin" or "agent".
SEED_USER_EMAIL="admin@example.com"
SEED_USER_PASSWORD="password123"
SEED_USER_NAME="Admin"
SEED_USER_ROLE="admin"
